server.port=9001
spring.application.name=StockPortfolioRiskAnalyzer

# Database - Updated to MySQL (Step 3 Requirement)
spring.datasource.url=jdbc:mysql://localhost:3306/stock_risk_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT (Technical Constraint Step 0.6)
# Ensure the secret is long enough for HS256
jwt.secret=mySecretKeyWithAtLeast256BitsForHS256Algorithm1234567890StockAnalyzer
jwt.validity=3600000

# Swagger (Requirement Step 6)
# Default path for springdoc v2 is /swagger-ui/index.html
springdoc.swagger-ui.path=/swagger-ui/index.html