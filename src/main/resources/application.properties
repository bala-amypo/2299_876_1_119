spring.application.name=demo
server.port=9001

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/transport?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration (REQUIRED to fix the 'Bean of type String' error)
jwt.secret=mySecretKeyWithAtLeast256BitsForHS256AlgorithmToWorkCorrectly12345
jwt.validity=3600000